export const metadata = {
  title: "About Sodfa | Coincidence Stories",
  description: "Learn about Sodfa, a platform for sharing real-life coincidence stories.",
};

export default function AboutLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
