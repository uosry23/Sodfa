export const metadata = {
  title: "Your Profile | Sodfa",
  description: "Manage your profile and stories on Sodfa.",
};

export default function ProfileLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
