export const metadata = {
  title: "Stories | Sodfa",
  description: "Read real stories of incredible coincidences shared by people around the world.",
};

export default function StoriesLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
