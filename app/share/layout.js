export const metadata = {
  title: "Share Your Story | Sodfa",
  description: "Share your own coincidence story with the Sodfa community.",
};

export default function ShareLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
