export const metadata = {
  title: "Log in to Sodfa",
  description: "Log in to share your coincidence stories and keep track of your submissions.",
};

export default function LoginLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
